import { ScrollView, StyleSheet, Text, View } from 'react-native';
import { AppShell, colors, SectionLabel, styles as shared } from './ui';
const people = [{ name: 'Miriam K.', detail: 'Writer · Nairobi', note: 'Finding grace in ordinary days.' }, { name: 'Samuel A.', detail: 'Youth leader · Accra', note: 'Learning to live with open hands.' }, { name: 'Grace N.', detail: 'Musician · London', note: 'Songs for the journey.' }];
export default function DiscoverScreen() { return <AppShell title="Discover"><ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={styles.content}><Text style={styles.intro}>Meet people who are walking a similar path.</Text><SectionLabel>People to know</SectionLabel>{people.map((person) => <View style={shared.card} key={person.name}><View style={styles.row}><View style={styles.avatar}><Text style={styles.avatarText}>{person.name[0]}</Text></View><View><Text style={styles.name}>{person.name}</Text><Text style={styles.detail}>{person.detail}</Text></View></View><Text style={styles.note}>{person.note}</Text></View>)}</ScrollView></AppShell>; }
const styles = StyleSheet.create({ content: { paddingTop: 24, paddingBottom: 24 }, intro: { color: colors.ink, fontSize: 21, lineHeight: 29, marginBottom: 30 }, row: { flexDirection: 'row', alignItems: 'center', marginBottom: 14 }, avatar: { width: 44, height: 44, borderRadius: 22, backgroundColor: '#D8C6A2', alignItems: 'center', justifyContent: 'center', marginRight: 12 }, avatarText: { color: colors.green, fontWeight: '800', fontSize: 18 }, name: { color: colors.ink, fontSize: 16, fontWeight: '700' }, detail: { color: colors.muted, marginTop: 3 }, note: { color: colors.ink, lineHeight: 22 } });
