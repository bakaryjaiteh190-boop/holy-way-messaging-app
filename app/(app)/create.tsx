import { useState } from 'react';
import { Alert, Pressable, StyleSheet, Text, TextInput, View } from 'react-native';
import { AppShell, colors, styles as shared } from './ui';
export default function CreateScreen() { const [text, setText] = useState(''); function publish() { if (!text.trim()) return; Alert.alert('Ready to share', 'Connect a posts table in Supabase to publish this reflection.'); setText(''); } return <AppShell title="Create"><View style={styles.content}><Text style={styles.prompt}>What is on your heart today?</Text><TextInput multiline value={text} onChangeText={setText} placeholder="Share a reflection, prayer, or encouragement..." placeholderTextColor={colors.muted} style={styles.input} textAlignVertical="top" /><Pressable onPress={publish} style={shared.button}><Text style={shared.buttonText}>Share with community</Text></Pressable></View></AppShell>; }
const styles = StyleSheet.create({ content: { paddingTop: 28 }, prompt: { color: colors.ink, fontSize: 22, fontWeight: '700', marginBottom: 18 }, input: { backgroundColor: colors.paper, borderColor: colors.line, borderWidth: 1, minHeight: 190, padding: 16, color: colors.ink, fontSize: 16, lineHeight: 24, marginBottom: 16 } });
